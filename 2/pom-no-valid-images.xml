<?xml version="1.0" encoding="UTF-8"?>
<project>
  <build>
    <plugins>
      <plugin>
        <configuration>
          <!-- нет домена — пропускается -->
          <image>nginx:1.2.3</image>
          <!-- нон-semver тег — пропускается -->
          <baseImage>gcr.io/distroless/java:latest</baseImage>
          <!-- нет version — пропускается -->
          <imageName>myregistry.io/myapp</imageName>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>
